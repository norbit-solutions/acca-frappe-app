# Python dependencies for ACCA LMS
# Mux SDK for video hosting and streaming (latest stable version)
mux_python>=5.0.0,<6.0.0

# JWT for signed playback URLs
PyJWT>=2.8.0,<3.0.0

# Cryptography for secure token generation (Frappe v16 requires 45.x)
cryptography>=45.0.0,<46.0.0
